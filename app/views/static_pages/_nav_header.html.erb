<div class="row">
  <div class="home-images">
    <div class="large-12 columns titlebar">
      <h2>Missing Persons Near
      <input value="<%= request_city %>">
      </h2>
    </div>
    <div class="large-12 columns">
      <div class="first-block block"></div>
      <% @kids.each_with_index do |kid, index| %>
        <% clazz = index >= 23 ? " last-row" : '' %>
        <a class="bw block<%= clazz %>" style="background-image: url('http://missingkids.com/<%=kid.image_url%>');"
          data-name="<%= "#{kid.first_name} #{kid.last_name}" %>" data-location="<%= kid.create_full_address %>"
          href="<%= kid.missing_url %>" target="_blank" data-timeago="<%= time_ago_in_words kid.missing_date %>"
          data-age="<%= kid.age %>"
          ></a>
      <% end %>
      <div class="block logo last-block"></div>
      <div class="popup"></div>
    </div>
  </div>
</div>
<div class="row news-row">
  <div class="large-12 columns newsfeed">
    <span><%= @kids.collect(&:description).join(" // ") %></span>
  </div>
</div>
<header class="row">
  <ul class="header-nav">
    <%= render "nav_links" %>
  </ul>
  <ul class="social">
    <li><a href="/404" class="google"></a></li>
    <li><a href="/404" class="twitter"></a></li>
    <li><a href="/404" class="facebook"></a></li>
    <li><a href="https://github.com/theverything/four04kids" class="github"></a></li>
  </ul>
</header>
